/*
 * Example module for exporting constants to use in multiple files.
 * (It is not required to have a config.js file -- this acts just like any other module)
 */

module.exports = {
    FOO: 42,
    GROUP_NAME: "Subscribers"
};